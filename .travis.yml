language: php

php:
  - 7.0
  - 7.1
  - 7.2
  - 7.3
  - nightly

install:
  - rm composer.lock
  - composer install

script:
  - vendor/bin/phpunit

after_success:
  - bash <(curl -s https://codecov.io/bash)
